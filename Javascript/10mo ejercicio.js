console.log ("Acceso a Evento Exclusivo")
console.log ("\nCondiciones: ser mayor de edad y poseer una entrada")
let edad = 19
let entrada = true

if (edad >= 18 && entrada) {
    console.log("\nAcceso concedido, cumple con todas las condiciones");
} else {
    console.log("\nAcceso denegado, no cumple con alguna de las condiciones");
}